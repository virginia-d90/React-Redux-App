
export const initialState = {
    catFacts: [],
    isFetchingData: false,
}

export const catReducer = (state = initialState, action) => {
    switch(action.type){
        
        
        default: 
            return state
    }
}